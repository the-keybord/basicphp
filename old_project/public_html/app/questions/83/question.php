
<div class="w-full max-w-2xl mx-auto p-4">
    <!-- The Question Card -->
    <div class='question'>
        <p></p>
        
        <pre>You are structuring a table in a relational database. The table will have a primary key.
For each statement, select True or False.
</pre>
    </div>
    <div class='options-container'>
        <div class="option">
            <code>Each value in a table must be unique.</code>
        </div><div class="option">
            <code>Each row in a table must be unique.</code>
        </div><div class="option">
            <code>Each column name in a table must be unique.</code>
        </div>
    </div>
    <div class='response hidden'>

    </div>
</div>
<?php require __DIR__ . '/../../drivers/true_driver.php'; ?>
