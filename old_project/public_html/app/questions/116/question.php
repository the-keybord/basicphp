
<div class="w-full max-w-2xl mx-auto p-4">
    <!-- The Question Card -->
    <div class='question'>
    <p>You have a database table for Inventory with the following requirements:</p>
    <ul>
        <li>-- ItemName must store a string of text.</li>
        <li>-- Quantity must be a whole number.</li>
        <li>-- Weight can have a single digit after the decimal point.</li>
    </ul>
    <pre>Move the appropriate data type from the list on the left to the correct column name on the right. 
You may use each data type once, more than once, or not at all.

Note: You will receive partial credit for each correct selection.

ItemName ___
Quantity ___
Weight ___

{{{ BIT |||
    DATETIME |||
    DECIMAL |||
    INT |||
    VARCHAR |||
    CHAR }}}</pre>
</div>

    <div class='response hidden'>

    </div>
</div>
<?php require __DIR__ . '/../../drivers/drag_driver.php'; ?>
